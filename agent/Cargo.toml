[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
agent-protocol = { path = "../agent-protocol" }
log = "0.4"
tokio = { version = "1.52", features = ["macros", "rt-multi-thread", "io-util", "process", "sync", "net", "time"] }
tokio-vsock = "0.7"
libc = "0.2"
